name: Build Multi-Platform

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0)'
        required: false
        default: 'v1.0.0'

jobs:
  build:
    strategy:
      fail-fast: false  # 允许其他平台继续即使一个失败
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            artifact_name: imagetrim
            artifact_archive: ImageTrim-1.0.0-linux.tar.gz
          - os: windows-latest
            platform: windows
            artifact_name: ImageTrim.exe
            artifact_archive: ImageTrim-1.0.0-win.zip
          - os: macos-latest
            platform: macos
            artifact_name: ImageTrim.app
            artifact_archive: ImageTrim-1.0.0-macos.tar.gz

    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup uv
      uses: astral-sh/setup-uv@v3
      with:
        version: "latest"

    - name: Create virtual environment
      run: uv venv

    - name: Install dependencies
      run: |
        uv pip install --upgrade pip
        uv pip install -r requirements.txt
        uv pip install pyinstaller

    - name: Install system dependencies (Linux)
      if: matrix.platform == 'linux'
      run: |
        sudo apt-get update
        # 基础依赖
        sudo apt-get install -y python3-tk python3-dev

        # 尝试安装 OpenGL 相关包（兼容不同版本）
        sudo apt-get install -y libgl1-mesa-glx || sudo apt-get install -y libgl1 || echo "OpenGL package not found"
        sudo apt-get install -y libglu1-mesa-dev || sudo apt-get install -y libglu1 || echo "GLU package not found"

        # X11 和图形库
        sudo apt-get install -y libglib2.0-0 libsm6 libxext6 libxrender-dev libgomp1 libxrandr2

        # 尝试安装其他依赖（可选）
        sudo apt-get install -y libxcb-xinerama0 || echo "xcb-xinerama not available"
        sudo apt-get install -y libxss1 || echo "xss not available"
        sudo apt-get install -y libdbus-1-3 || echo "dbus not available"
        sudo apt-get install -y libgtk-3-0 || echo "gtk3 not available"
        sudo apt-get install -y libasound2-dev || echo "alsa not available"

    - name: Install system dependencies (macOS)
      if: matrix.platform == 'macos'
      run: |
        # macOS 系统依赖通常已包含在系统中
        echo "macOS dependencies check"

    - name: Create build directories
      run: |
        mkdir -p build/macos
        mkdir -p build/linux

    # Info.plist will be handled by PyInstaller automatically

    - name: Create Linux .desktop file
      if: matrix.platform == 'linux'
      run: |
        cat > build/linux/imagetrim.desktop << 'EOF'
        [Desktop Entry]
        Name=ImageTrim
        Name[zh_CN]=图片去重工具
        Comment=Image deduplication and conversion tool
        Comment[zh_CN]=图片去重和格式转换工具
        Exec=imagetrim
        Icon=imagetrim
        Terminal=false
        Type=Application
        Categories=Graphics;Photography;
        MimeType=image/jpeg;image/png;image/webp;image/avif;
        StartupWMClass=ImageTrim
        EOF

    - name: Build application
      shell: bash
      run: |
        echo "Current directory contents:"
        ls -la
        echo "App directory contents:"
        ls -la app/ || echo "App directory not found"
        echo "Starting build with simplified script..."
        if [ "${{ matrix.platform }}" = "windows" ]; then
          .venv/Scripts/python build_simple.py
        else
          .venv/bin/python build_simple.py
        fi

    - name: Create additional packages (Linux)
      if: matrix.platform == 'linux'
      run: |
        # Check if build succeeded first
        if [ ! -f "dist/imagetrim" ]; then
          echo "Error: Linux binary not found in dist/"
          ls -la dist/ || echo "dist directory not found"
          exit 1
        fi

        # Create AppImage
        mkdir -p ImageTrim.AppDir/usr/bin
        mkdir -p ImageTrim.AppDir/usr/share/applications
        mkdir -p ImageTrim.AppDir/usr/share/icons/hicolor/256x256/apps

        cp dist/imagetrim ImageTrim.AppDir/usr/bin/

        # Create desktop file directly in AppDir (don't rely on build/linux)
        cat > ImageTrim.AppDir/usr/share/applications/imagetrim.desktop << 'EOF'
        [Desktop Entry]
        Name=ImageTrim
        Name[zh_CN]=图片去重工具
        Comment=Image deduplication and conversion tool
        Comment[zh_CN]=图片去重和格式转换工具
        Exec=imagetrim
        Icon=imagetrim
        Terminal=false
        Type=Application
        Categories=Graphics;Photography;
        MimeType=image/jpeg;image/png;image/webp;image/avif;
        StartupWMClass=ImageTrim
        EOF

        # Create AppRun
        cat > ImageTrim.AppDir/AppRun << 'EOFRUN'
        #!/bin/sh
        HERE="$(dirname "$(readlink -f "${0}")")"
        export LD_LIBRARY_PATH="${HERE}/usr/lib:${HERE}/usr/lib/x86_64-linux-gnu:${LD_LIBRARY_PATH}"
        export PATH="${HERE}/usr/bin:${PATH}"
        exec "${HERE}/usr/bin/imagetrim" "$@"
        EOFRUN
        chmod +x ImageTrim.AppDir/AppRun

        # Download appimagetool
        wget -q https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-x86_64.AppImage -O appimagetool
        chmod +x appimagetool

        # Verify desktop file exists
        echo "Checking desktop file location:"
        ls -la ImageTrim.AppDir/usr/share/applications/
        echo "Checking AppDir structure:"
        ls -la ImageTrim.AppDir/

        # Create desktop file also at root level (some versions of appimagetool require this)
        cp ImageTrim.AppDir/usr/share/applications/imagetrim.desktop ImageTrim.AppDir/imagetrim.desktop

        echo "Creating AppImage with verbose output..."
        ./appimagetool --verbose ImageTrim.AppDir ImageTrim-1.0.7-linux.AppImage || {
          echo "AppImage creation failed, trying alternative approach..."
          # Try without desktop file requirement
          ./appimagetool --no-appstream ImageTrim.AppDir ImageTrim-1.0.7-linux.AppImage || {
            echo "Final attempt: creating AppImage manually..."
            # Create a simple AppImage without desktop integration
            mkdir -p ImageTrim.AppDir/usr/bin
            cat > ImageTrim.AppDir/AppRun << 'EOF'
#!/bin/sh
HERE="$(dirname "$(readlink -f "${0}")")"
export PATH="${HERE}/usr/bin:${PATH}"
exec "${HERE}/usr/bin/imagetrim" "$@"
EOF
            chmod +x ImageTrim.AppDir/AppRun
            ./appimagetool --no-appstream ImageTrim.AppDir ImageTrim-1.0.7-linux.AppImage
          }
        }

    - name: Create DMG (macOS)
      if: matrix.platform == 'macos'
      run: |
        if [ -f "dist/ImageTrim.app" ]; then
          mkdir -p dmg_temp
          cp -R "dist/ImageTrim.app" dmg_temp/
          ln -s /Applications dmg_temp/Applications
          hdiutil create -volname "ImageTrim" -srcfolder dmg_temp -ov -format UDZO ImageTrim-1.0.0-macos.dmg
          rm -rf dmg_temp
        fi

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.platform }}-build
        path: |
          dist/
          archives/
          *.AppImage
          *.dmg
        retention-days: 30

  create-release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Delete existing Release (if exists)
      run: |
        TAG_NAME="${GITHUB_REF#refs/tags/}"
        if gh release view "$TAG_NAME" >/dev/null 2>&1; then
          echo "Deleting existing release: $TAG_NAME"
          gh release delete "$TAG_NAME" --yes
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Create/Update Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          artifacts/windows-build/dist/*.exe
          artifacts/windows-build/archives/*.zip
          artifacts/macos-build/dist/*.app/**
          artifacts/macos-build/archives/*.tar.gz
          artifacts/macos-build/*.dmg
          artifacts/linux-build/dist/imagetrim
          artifacts/linux-build/archives/*.tar.gz
          artifacts/linux-build/*.AppImage
        draft: false
        prerelease: false
        generate_release_notes: true
        name: "ImageTrim ${{ github.ref_name }}"
        body: |
          ## ImageTrim ${{ github.ref_name }} Release

          ### Main Features
          - Image deduplication functionality
          - AVIF format conversion
          - Modern UI interface

          ### Cross-Platform Support
          - macOS application bundle (.app)
          - Linux executable and AppImage

          ### Download Files
          - **Windows**: `ImageTrim.exe`
          - **macOS**: `ImageTrim.app` or `.dmg` installer package
          - **Linux**: `imagetrim` executable or `.AppImage`

          ### Usage Instructions
          1. Download the file for your platform
          2. Windows/macOS: Double-click to run
          3. Linux: `chmod +x imagetrim && ./imagetrim`

          ---
          Automatically built with GitHub Actions
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}